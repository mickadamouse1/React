import React from 'react';

const Icon = (props) => {

    const icon = {
        fontSize: '2.5rem'
    }

    return (
        <i className="fas fa-cloud" style={icon}></i> 
    );
}

export default Icon;